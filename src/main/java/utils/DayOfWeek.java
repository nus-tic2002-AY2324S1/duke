package utils;

/**
 * This enum class contains all the days of the week.
 */
public enum DayOfWeek {
    SUN, MON, TUE, WED, THU, FRI, SAT, SUNDAY,
    MONDAY, TUESDAY, WEDNESDAY, THURSDAY, FRIDAY, SATURDAY;
}

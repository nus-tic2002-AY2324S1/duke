package task;

/**
 * This enum class contains all the keywords that the user can use for the command.
 */
public enum Keyword {
    LIST, BLAH, BYE, MARK, UNMARK, DELETE, FIND,
    HELP, TODO, DEADLINE, EVENT, SORT, UNDO,
}
